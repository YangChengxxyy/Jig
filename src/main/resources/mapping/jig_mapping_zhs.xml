<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jig.mapper.JigMapper_zhs">
    <insert id="addShoplist">
        insert into purchase_incoming_submit(submit_id,code,count,submit_time,status,production_line_id,bill_no)
        values (#{submit_id},#{code},#{count},#{submit_time},0,
        select id from production_line where name = #{production_line_id},
        #{bill_no})

    </insert>
    <select id="get_manager_purchaseCheck_list" resultType="com.jig.entity.PurchaseIncomeSubmit">
        select * from purchase_incoming_submit p inner join user u on p.submit_id = u.id
        where p.status = 2
    </select>
    <select id="get_manager_purchase_detail" resultType="com.jig.entity.PurchaseIncomeSubmit">
        select * from purchase_incoming_submit p inner join user u on p.submit_id = u.id
        where p.id = #{id}
    </select>
    <update id="manager_first_pass_purchase_submit">
        update purchase_incoming_submit
        <if test="pass==3">
            set status = 3
        </if>
        <if test="pass==4">
            set status = 4
        </if>
        where id = #{id}
    </update>
    <select id="get_manager_jig_info_list" resultType="com.jig.entity.JigDefinition">
        select * from jig_definition j inner join jig_family f on j.family_id = f.id
        limit #{page_number},5
    </select>
    <select id="get_manager_jig_info_list_pages" resultType="java.lang.Integer">
        SELECT count(*) from jig_definition
    </select>
    <select id="get_manager_purchaselist_history" resultType="com.jig.entity.PurchaseIncomeSubmit">
        select * from purchase_incoming_submit p inner join user u on p.submit_id = u.id
        <where>
            <if test="bill_no!=''">
                and p.bill_no like concat('%',#{bill_no},'%')
            </if>
            <if test="submit_name!=''">
                and u.name like concat('%',#{submit_name},'%')
            </if>
            <if test="status!=''">
                and p.status = #{status}
            </if>
            <if test="start_date!='' and end_date !=''">
                and (STR_TO_DATE(p.submit_time,'%Y-%m-%d') BETWEEN '${start_date}' AND '${end_date}')
            </if>
        </where>
    </select>
    <select id="get_manager_purchaselist_history_pages" resultType="java.lang.Integer">
        select count(*) from purchase_incoming_submit p inner join user u on p.submit_id = u.id
        <where>
            <if test="bill_no!=''">
                and p.bill_no like concat('%',#{bill_no},'%')
            </if>
            <if test="submit_name!=''">
                and u.name like concat('%',#{submit_name},'%')
            </if>
            <if test="status!=''">
                and p.status = #{status}
            </if>
            <if test="start_date!='' and end_date !=''">
                and (STR_TO_DATE(p.submit_time,'%Y-%m-%d') BETWEEN '${start_date}' AND '${end_date}')
            </if>
        </where>
    </select>
</mapper>
